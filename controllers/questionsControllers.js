const Question = require('../models/Question')

const questionsControllers = {
    getQuestion: async (req, res) => {
        try {
            // let question = Question.f

        } catch (e) {

        }
    }
}

module.exports = questionsControllers